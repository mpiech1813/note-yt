// this will send the data

// const title = currentWindow.getSelection();
// Zaina wrote a firefox extension
// started with react but moved away
// popup scrip or background script

// got the info in the content script used a document.quearyselector

// use runtime connection content >> background >> popup

//menu-remove-element

// const title = document.querySelector(
//   '.title style-scope ytd-video-primary-info-renderer'
// );

// function showButton(tabId) {
//   console.log(tabId);
//   browser.pageAction.show(tabId);
// }

// browser.tabs.onUpdated.addListener(showButton);

// function sendData(tab) {
//   browser.tabs.sendMessage(tab.id, { data: 'dummyData' });
// }

// // function thisTab(tab) {
// //   console.log(tab);
// // }

// const dataToContent = (tab) => {
//   browser.tabs.sendMessage(tab.id, { data: 'dummyData' });
// };

// browser.pageAction.onClicked.addListener(sendData);

// browser.runtime.onMessage.addListener((message) => {
//   console.log(message);
//   // dataToContent(6);
// });
